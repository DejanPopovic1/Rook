#ifndef CHANGE_STATE_H
#define CHANGE_STATE_H

#include "state.h"
#include <string>
#include <vector>

using std::string;
using std::vector;

class ChangeState{
public:
    ChangeState();



private:
    vector<string> validMoves;
};

#endif //CHANGE_STATE_H
